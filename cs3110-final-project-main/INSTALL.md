'Battleship' can be run by, after downloading the project, opening it and
calling [dune build]. Once the project is built, it can be ran using 
[make play]. We have no extra OPAM packages needed to install.